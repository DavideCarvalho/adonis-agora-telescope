---
'@adonis-agora/telescope-ui': patch
---

Routine dependency refresh. The only change that reaches a published manifest is `@base-ui/react` moving from `1.7.0` to `1.8.0` in `@adonis-agora/telescope-ui`'s runtime dependencies; everything else in the sweep is devDependency-only.
